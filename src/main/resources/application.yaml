spring:
  application:
    name: Vibe Studio
    description: this is application

  datasource:
    url: jdbc:postgresql://localhost:5432/vibe_studio
    username: postgres
    password: Dilip@1586
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

