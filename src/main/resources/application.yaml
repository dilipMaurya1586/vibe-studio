spring:
  application:
    name: Vibe Studio
    description: this is application

  datasource:
    url: jdbc:postgresql://localhost:5432/vibe_studio
    username: postgres
    password: Dilip@1586
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  ai:
    openai:
      api-key:
      base-url: https://openrouter.ai/api
      chat:
        options:
          #          model: x-ai/grok-code-fast-1
          model: google/gemini-3-flash-preview
          temperature: 0.0


jwt:
  secret-key: as9b7aodlbjfkluosfd789ujeklfnlsduf90b8ha908w74tuqijvlckajdl

stripe:
  secret: ${STRIPE_SECRET_KEY}
#  secret: sk_test_51ReZ5jRl21PQC0VyizRuRUdDKJyTAshaVT96OsZYGMRToQ405xWjw268ycZOQD80048zazwfMT4FUvf0SsuukF8z00FukHYpYs
  webhook:
    secret:

client.url: http://localhost:8080

logging:
  level:
    org.springframework.ai:
      chat.client: DEBUG

minio:
  url: http://localhost:9000          # or minio:9000 if inside docker network
  access-key: minioadmin
  secret-key: minioadmin123
  project-bucket: projects